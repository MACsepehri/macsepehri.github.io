import { BaseSection, Section } from "../../public/style/StyleComponents";

export default function Box({isLogin}) {
    return (
        <BaseSection>
            <Section>
                
            </Section>
        </BaseSection>
    )
}